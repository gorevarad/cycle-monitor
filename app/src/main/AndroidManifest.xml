<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location: fine accuracy is required for a cycling computer; background is required so a
         ride keeps recording if the screen turns off or another app takes focus mid-ride. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Keeps GPS sampling alive through Doze while a ride is actively RIDING; acquired/released
         around the ride lifecycle only, not held permanently -- see RideRecordingService. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Map tiles, routing, and (future) cloud sync all need network; ride recording itself does
         not and must keep working fully offline. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <application
        android:name=".CycleMonitorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CycleMonitor">

        <!-- Google Maps Platform API key. Populate MAPS_API_KEY in local.properties (gitignored);
             an empty key here just means the map panel shows its "unavailable" fallback -- see
             app/build.gradle.kts and map/MapAvailability.kt. Never commit a real key. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CycleMonitor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ride.service.RideRecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
